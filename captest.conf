pre-start script

bash << "EOF"
  mkdir -p /var/log/captest
  chown -R cjiang /var/log/captest
EOF

end script

description "Captest"

start on runlevel [2345]

stop on runlevel [016]

#
chdir /var/www/rubydev.aicure.com/captest/current

setuid cjiang  # set to the user, which should run the app
exec echo 20120313 | rvmsudo bundle exec unicorn_rails -l 8081 -c config/unicorn.rb -E production -D

respawn
## give up if I respawn 3 times in 60 seconds...
respawn limit 3 60